# ---- OpenAI ----
OPENAI_API_KEY=
# Optional, defaults to gpt-4o-mini
OPENAI_MODEL=gpt-4o-mini

# ---- Freshness ----
# Only include items published within this many days. Default: 7.
FRESHNESS_DAYS=7

# ---- Air Quality (optional) ----
# Free token from https://aqicn.org/data-platform/token/ — powers the
# "מצב הסביבה בישראל השבוע" section. If empty, that section shows
# "נתוני איכות אוויר אינם זמינים כרגע" and the report still generates.
WAQI_TOKEN=

# ---- Gmail SMTP ----
# For Gmail use an "App Password" (not your normal password):
# https://myaccount.google.com/apppasswords
SMTP_HOST=smtp.gmail.com
SMTP_PORT=587
SMTP_USER=your-address@gmail.com
SMTP_PASS=your-16-char-app-password
EMAIL_FROM=your-address@gmail.com
EMAIL_TO=lirankor@gmail.com

# ---- Safety switch ----
# Emails are NEVER sent unless this is exactly "true".
SEND_EMAIL=false
