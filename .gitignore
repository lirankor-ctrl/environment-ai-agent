node_modules/
.env
*.log

# Generated reports are kept out of git by default; remove the next line if you
# prefer to commit them. The GitHub Action commits data/seen.json so dedupe
# persists between runs.
reports/
